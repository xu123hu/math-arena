# 测试报告

> 测试时间：2026-08-15（迭代19 收尾轮）→ 2026-08-16（迭代20 收尾轮）｜ 测试环境：Windows 本机（Postgres 54329 容器 / Redis 6379 / MinIO 9000 / 后端 :8000 / 前端 vite :5176 默认真实后端）

## 0.0 迭代20 收尾轮（2026-08-16）测试总览

| 项 | 结果 |
|------|------|
| 全量 pytest | **806 passed, 1 skipped, 0 failed**（修复前 804 passed / 2 failed，均为 test_iter05_files.py 契约过期用例） |
| RAG 向量召回 unpack 修复验证 | ✅ 真实 chunk embedding 作查询向量：召回 20 条，top1=目标 chunk，raw_score=1.0 |
| 显式 spark_vl 引擎解析污染修复 | ✅ `_dispatch_parse` 解包 `(text, confidence)`，test_iter05_files.py 10 passed |
| 端到端冒烟（真实后端 + 真实库） | ✅ 登录/知识图谱/专练5题(bank5+ai0)/每日一题/全真模拟组卷/学情报告/管家周报 全 code=0 |
| TAL 难度再平衡 | ✅ 4668/4668 全量打标（easy 1912 / medium 2103 / hard 653） |

## 0. 本轮（迭代19 收尾）测试总览

| 套件/验收 | 用例数 | 结果 |
|------|------|------|
| tests/test_figure_frames.py（F13 分步渲染） | 14 | ✅ |
| tests/test_figure_block.py（figure 事件契约+重放） | 15 | ✅ |
| tests/test_socratic_figures.py（图形规划/发射策略/全旅程） | 19 | ✅ |
| tests/test_error_dedup.py（错题去重） | 5 | ✅ |
| tests/test_bkt.py（BKT 接口预留） | 7 | ✅ |
| 回归：socratic_solver + figure_renderer + student_linkage + mock_exam + student_pipeline | 150+ | ✅ |
| AI 管家 3 闭环 ×5（真实 LLM） | 15 | ✅ 14/15（闭环1 5/5、闭环2 4/5、闭环3 5/5） |
| 拍照题全链路（MinIO+云轨） | 1 | ✅（spark_vl，见 §2.4） |
| 性能探测（10 接口 ×30） | 300 请求 | ✅ P95 全部 <200ms |

## 1. 单元测试（pytest）

测试文件：services/api/tests/test_core_services.py

| 模块 | 用例 | 结果 |
|------|------|------|
| FSRS 分级（S 分级阈值 new/lv1-lv4） | 9 参数化 + 3 联动 | ✅ 全过 |
| FSRS 数学（R 曲线/到期反解/筛选档位） | 3 | ✅ 全过 |
| 防幻觉评分（C 类分段扣分/余弦） | 11 | ✅ 全过 |
| 学情聚合（综合分/错因细分） | 3 | ✅ 全过 |
| 平台意图匹配（页面直达/练题意图） | 4 | ✅ 全过 |
| **合计** | **33** | **33 passed** ✅ |

## 2. 接口端到端测试

### 2.1 核心接口

| 用例 | 前置 | 步骤 | 预期 | 结果 |
|------|------|------|------|------|
| 登录链路 | 新手机号 | sms-code → login | code=0 + token | ✅ |
| practice/start 专练 | 登录 | special kp=函数 count=5 | 5 道 GAOKAO-Bench 真题（source 标注，ai_generated=false） | ✅ |
| practice/start 参数校验 | 登录 | count=3 | 40001 拒绝（专题训练支持 5~30 题） | ✅ |
| practice/start 每日一题 | 登录 | daily | 1 题（薄弱点加权选题） | ✅ |
| practice/start 错题重练 | 无错题新用户 | retry | 50301 无错题记录 | ✅ |
| exam/generate 全真模拟 | 登录 | full_mock | 150 分/120 分钟/16 题，bank13+ai3 | ✅ |
| weekly-report / growth/panel / weak-points | 登录 | GET×3 | 全 200，无变量泄漏 | ✅ |

### 2.2 FSRS 闭环（迭代18 修复验证）

| 用例 | 步骤 | 预期 | 结果 |
|------|------|------|------|
| 新收录错题等级 | 收录错题 → 详情 | fsrs_level=new（S=0.4），非「非常稳」 | ✅ |
| 复习推进 | review remembered | S=1.0 → lv1，next_review 延后 3 天 | ✅ |
| 复习失败重置 | review forgotten | S=0.34 → new，review_count=0，明天复习，wrong_count+1 | ✅ |
| 稳定性筛选 | filter stability=critical | 包含新错题（new→critical） | ✅ |
| 热力图 | 有错题用户 | 格子按 S 分级（new），非整图 decay | ✅ |
| UI 展示 | 浏览器 | 列表徽章「刚收录」、详情「S≈0.4/记忆等级·刚收录」、图例含刚收录 | ✅ |

### 2.3 AI 管家最小闭环（迭代18 修复验证 + 迭代19 5×验收）

| 用例 | 步骤 | 预期 | 结果 |
|------|------|------|------|
| 查学情「我哪部分最弱」 | SSE 对话 ×3 | 回复列出真实薄弱点（集合20%/复数23%/弧度制26%），与 weak-points 一致 | ✅ 3/3 |
| 出题「给我出3道导数题」 | SSE ×2 | smart_quiz 卡片 3 道导数题（含 LaTeX） | ✅ 2/2 |
| 跳转「打开错题本」 | SSE ×3 | action open_page /student/error-book，0ms 零 LLM，无幻觉文本 | ✅ 3/3 |
| 免责尾巴清洗 | 全部对话 | 回复尾不含「部分内容可能由AI生成」 | ✅ |
| **迭代19 连续 5 次验收** | 同上三闭环各 ×5（真实 LLM + 真实数据种子） | 见下表 | ✅ 14/15 |

迭代19 5×验收明细（scripts/verify_butler_loops.py，落盘 deliverables/butler-loop-verification.txt）：

| 闭环 | 结果 | 备注 |
|------|------|------|
| 1 我哪部分最弱 | **5/5** | 每次回复均列出真实薄弱点「函数的概念与基本初等函数 · 掌握度 12%」 |
| 2 给我出3道导数题 | **4/5** | 4 次 3 题；1 次 LLM 出题仅过闸 2 题（出题数量稳定性待平台侧样本优化，非路由故障） |
| 3 打开错题本 | **5/5** | 5 次均 matched + route=/errors + 到期数 |

### 2.4 拍照题全链路（迭代19 修复后）

| 用例 | 步骤 | 预期 | 结果 |
|------|------|------|------|
| 本地轨 | 生成题目图 → upload → PUT → complete → parse → 轮询 | status=parsed + 文本提取 | ✅（complete 返回 200，修复前 50001） |
| 云轨 | 同上（question_photo） | wf_doc_understand LaTeX+confidence | ✅ **迭代19 修复（链路级 PASS）**：星辰文件服务 404 → 改 data URI 直传 + 工作流输出 Python 布尔容错 + 超时 30s→90s，实测全链路 status=parsed、engine=spark_vl、工作流返回 question_text+latex_fragments+confidence。⚠️ 识别内容质量受合成测试图限制（resvg 渲染中文字形与真实拍照差异），建议用真实拍照题做最终内容验收 |

### 2.5 性能（迭代19 复测：10 接口 × 30 次，数据最多用户，目标 P95 < 200ms）

| 接口 | P50 | P95 | 判定 |
|------|-----|-----|------|
| health | 5.8ms | 9.0ms | ✅ |
| 错题列表 | 20.3ms | 25.7ms | ✅ |
| 错题复习计划 | 21.9ms | 40.9ms | ✅ |
| 学情亮点 / 薄弱点 | 8.5 / 9.3ms | 10.8 / 11.7ms | ✅ |
| 训练组推荐 / 掌握度汇总 | 18.0 / 23.0ms | 22.7 / 31.3ms | ✅ |
| 知识图谱 / 会话列表 | 22.5 / 18.8ms | 27.5 / 22.1ms | ✅ |
| 管家面板 | 36.2ms | 43.5ms | ✅（MAX 22.5s 为缓存过期回填 LLM，一次性） |

**300 次请求，P95 ≥200ms 接口 0 个。** 索引优化：错题列表原走 Bitmap Heap Scan + Sort，
新增复合索引 idx_error_records_user_deleted_created（m2_017）；错题去重唯一索引（m2_016）确认生效。

## 3. 前端验证

- vite 生产构建：139 模块，6.31s，0 错误 ✅
- 4 个改动模块 dev 编译 200 ✅
- 浏览器实测：登录 → 错题本 70 道 + 热力图 + 「刚收录」徽章 + 记忆状态 S≈0.4 ✅
- 浏览器内 fetch 验证：filter 接口 total=70、token 正常 ✅
- 遗留观察：playwright 会话内多次 goto 后页面组件偶发停留在加载态（API/模块级验证均正常，判定为 playwright 环境状态问题，非产品缺陷；真实浏览器首次加载正常）

## 4. 影子评测（意图路由）

- router_eval_logs：52 条，agree 36 / disagree 16 = 69.2% 命中率。
- 分歧模式：星辰 intent_router 将「给我出3道导数题」判为 chat（本地 smart_quiz 正确出题）。
- 结论：本地路由正确率可靠；星辰 intent_router 对出题类意图判别偏弱，建议队友在平台侧补充出题类样本。

## 5. 已知问题与阻塞

| 问题 | 状态 | 责任方 |
|------|------|--------|
| 星辰 App A 五流 20373 授权/额度 | 阻塞中 | 平台侧/队友 |
| solution_pregrade 20805 输出绑定 | 阻塞中 | 平台侧 |
| ~~星辰文件上传端点 404（云轨拍照解析）~~ | ✅ 迭代19 已绕过（data URI 直传） | — |
| wf_socratic_chat 不支持透传 system messages | 已停用 chat 云轨，MiMo 主力 | 平台侧修好后可恢复 |
| smart_quiz 出题数量偶发少 1 题（5 次中 1 次 2 题） | 观察中（4/5 达标） | 平台侧样本可优化 |
| ~~RAG 向量召回报 not enough values to unpack（vec_count=0，trgm 兜底生效）~~ | ✅ 迭代20 已修复（select 补 distance 列 + 端到端验证） | 本侧 |

## 6. 本轮（迭代19）验收脚本产物

- `deliverables/butler-loop-verification.txt`：管家 3 闭环 ×5 完整实录（14/15 通过）；
- `scripts/verify_butler_loops.py` / `scripts/verify_photo_chain.py` / `scripts/perf_probe.py`：
  可重复执行的验收脚本（含自动数据准备与清理）。
