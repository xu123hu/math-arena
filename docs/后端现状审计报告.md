# 后端现状审计报告

> 生成日期：2026-08-15 ｜ 审计范围：智学数研学生端 M2 后端（D:\math-arena\services\api）
> 审计方法：真实数据库查询 + 接口实测 + 代码走查 + 浏览器 UI 验证

## 1. 数据地基（与交接基线一致）

| 数据 | 数量 | 说明 |
|------|------|------|
| 题库 question_bank | 5689 | 有效题（软删外） |
| 知识点 knowledge_points | 52 | 覆盖 12 模块 |
| 用户 users | 9004 | |
| 错题 error_records | 525 | 分布 380 用户 |
| chunks / knowledge_docs | 10545 / 55 | RAG 知识库 |

难度分布：easy 4159 / medium 1220 / hard 310（高考真题 905 道已重标：easy 314 / medium 363 / hard 228；TAL 4668 道重标进行中，见 §5）。

## 2. 核心功能实测（全部 200）

| 接口 | 结果 |
|------|------|
| POST /api/student/practice/start（special 5 题） | ✅ 200，返回 GAOKAO-Bench 真题（含来源标注），bank=5 ai=0 |
| POST /api/student/practice/start（daily） | ✅ 200，每日一题（薄弱点加权选题） |
| POST /api/student/exam/generate（full_mock） | ✅ 200，150 分/120 分钟/16 题（8选择+3填空+5解答），bank13+ai3 |
| GET /api/butler/weekly-report | ✅ 200 |
| GET /api/student/growth/panel | ✅ 200 |
| GET /api/student/report/weak-points | ✅ 200，与画像卡数据一致 |

## 3. 迭代19 收尾轮修复清单

### 3.0 学情报告展示修复（任务1/2）
- 亮点图标英文变量名（trend_up/streak/review_done）改为中文（进步趋势/连续学习/复习完成）；
  前端 ReportView 增加旧键→中文的防御映射（兼容缓存窗口）；
- 周报标题「小婷的周报」→「{当前用户昵称}的周报」（auth 昵称，空昵称兜底"我的学习周报"）。

### 3.1 前端默认真实后端（任务3）
- vite.config.js 默认代理 127.0.0.1:8000；mock 改为显式开关 `VITE_USE_MOCK=1`（VITE_REAL_API 保留无副作用）。
- 处置：5176 端口上残留的旧配置 dev server（mock 默认）已终止，避免用户继续看到假数据。

### 3.2 错题去重（任务4）
- 应用层：三处收录入口统一 `_upsert_error_record`（同用户同题干全时段唯一；重复收录
  wrong_count+1、补全缺失字段；savepoint 兜底并发冲突）；AI 初判/记忆联动仅新收录触发。
- 数据库：m2_016 唯一索引（user_id, md5(btrim(question_text))) WHERE deleted_at IS NULL；
  迁移内联数据清理合并**开发库 29 条重复错题**（保留 wrong_count 最大者，聚合计数，其余软删）。
- 脚本：scripts/dedup_error_records.py（幂等、--dry-run）。测试 5 例全过，学生链路回归 79 例全过。

### 3.3 FSRS 分级（任务5）
- 复核确认迭代18 已修复：fsrs_level 按稳定度 S 分级，新题（S=0.4）→「刚收录」，33 例单测全过，前端 new→「刚收录」映射正确。本轮无新增改动。

### 3.4 AI 管家闭环（任务6）
- 3 闭环 ×5 次真实 LLM 验收（scripts/verify_butler_loops.py，真实数据种子）：
  查学情 5/5（回复列出真实薄弱点）、出题 4/5（一次仅 2 题，数量稳定性待优化）、
  打开错题本 5/5（route-intent → /errors）。结论：闭环可用，无需修路由。

### 3.5 前端修复包（任务7）
- 复核确认迭代18 已完成：butlerApi.errorDetail/errorTutor 存在且接线；GraphView「直接练」
  带 ?kp=；PracticeView 消费 kp 定向出题（group-recommend 支持 kp_code）；5题vs1题空态
  已在后端修复（新用户返回摸底知识点走 special 5 题）。本轮零改动，仅核对。

### 3.6 BKT 决策（任务8）
- SQL 实测：mastery_records 每用户答题量中位 **2 题**（49 用户，p90=9，≥20 题仅 2 人）→
  **降级为接口预留**（阈值 20）：新增 app/services/bkt.py（四参数经验值 pL0/pT/pG/pS +
  bkt_update/bkt_mastery_after_block + should_use_bkt），7 例单测过；热路径维持 BKT-lite
  （简单正确率后验）。数据达标后仅需替换 _update_mastery 更新式。

### 3.7 MinIO + 拍照题（任务9）
- MinIO 容器确认运行（healthy）；Redis 容器已拉起。
- **修复云轨**：①星辰文件上传服务 /workflow/v1/files 全路径探测 404（平台已下线）→ 改
  **data URI 直传**（实测工作流 code=0）；②工作流输出含 Python 风格布尔（False）导致
  JSON 解析失败 → _parse_output 增加保守归一化（True/False/None→小写，BOM 剥离）；
  ③wf_doc_understand 超时 30s→90s（大图 base64 处理慢）。
- 全链路验收（upload→MinIO PUT→complete→parse→轮询）：status=parsed、engine=spark_vl ✅；
  云轨输出解析（question_text/latex_fragments/confidence）链路打通；合成测试图的 VL 识别
  内容质量有限（resvg 渲染字形与真实拍照差异），真实拍照题为最终验收介质。

### 3.8 性能（任务10）
- 10 关键接口 ×30 次实测：**P95 全部 <200ms**（最高 43.5ms），300 请求零超限；
- EXPLAIN 检查：错题列表 Bitmap Heap Scan+Sort → m2_017 复合索引
  (user_id, deleted_at, created_at DESC) 一次覆盖过滤与排序（预留给数据增长）；
- pg_stat_statements 未启用（可选开启）；RAG 向量召回有一条 unpack 错误（trgm 兜底生效，待查）。

### 3.9 F13 可视化讲解（前序任务，随本轮一并归档）
- figure_renderer 分步渲染 + figure SSE 事件 + socratic 图形规划器 + MathFigure.vue
  前端已应用到 D:\frontend（构建通过）；49 例 F13 测试全过；
- 交付物：deliverables/f13-visual-guidance/（调研/设计/实现/验证 + 效果图 + preview.html）。

## 4. 迭代18 修复清单### 3.1 FSRS 展示 bug（任务1）
- **根因**：fsrs_level 用可提取性 R 分级，新题 R=1.0 被标 lv4「最稳」；student_router 复习完成回包还硬编码 fsrs_level(1.0)。
- **修复**：改为稳定度 S 分级（阈值 1/3/7/15 对齐复习档），新增 new 档=刚收录/待复习；热力图格子同步改 S 分级（顺带修复「到期日 R 恒等于阈值导致整图 decay」的隐藏 bug）；前端 te()/he()/图例/CSS 增加「刚收录」。
- **决策**：真 FSRS v4/v5 17 参数降级为后续优化项。依据：错题中位 1 题/用户（p90=1），复习事件仅 71 次，远不足以训练参数；现有启发式（分段 S + 答错惩罚 + R 遗忘曲线）继续服务。

### 3.2 AI 管家最小闭环（任务2）
- **根因**：agent_router 在 chat 意图 + 星辰开关开启时走 wf_socratic_chat 工作流，只传 question——学情画像卡/平台地图/记忆全部被丢弃：①「我哪部分最弱」回复泛化；②「打开错题本」无 action（模型幻觉「已为你打开」）；③回复尾带「部分内容可能由AI生成」脏尾巴。
- **修复**：①停用 chat 场景星辰流，chat 固定走本地 skill（MiMo，完整上下文注入），_socratic_chat_stream 保留作恢复通道；②平台页面直达提前到 precheck 管线（page_intent 阶段，零 LLM、确定性命中，落库可回放）；③补学情关键词（「哪部分最弱」族）；④deepseek provider 流末缓冲 + guard 落库双层清洗免责尾巴。
- **实测**：学情 3/3 回复列出真实薄弱点（与 weak-points 一致）；出题 2/2 smart_quiz 真实题目卡片；跳转 3/3 open_page action（0ms，零 LLM）。
- **影子评测**：router_eval_logs 52 条，agree 36/16 = 69.2%；分歧集中在星辰 intent_router 把「出题」判为 chat（本地 smart_quiz 正确）。

### 3.3 前端修复包（任务3）
- butlerApi 补 errorDetail/errorTutor（错题 AI 正解与答疑此前因方法缺失静默失败）。
- GraphView「直接练」带 ?kp= 参数；PracticeView 消费 query.kp 定向出题。
- 「5 题 vs 1 题」：group-recommend 新用户空态返回默认摸底知识点（集合），前端走 special 5 题，卡片与实际题量一致。
- 生产构建通过（139 模块，6.31s）。

### 3.4 MinIO + 拍照题（任务4）
- 本地轨全链路验证通过：upload → PUT(预签名) → complete → parse → RapidOCR 文本提取。
- 修复：非分片直传的 complete 误走分片合并报 50001（现幂等确认）；星辰 upload_file 超时构造缺 pool 参数已修；question_photo 优先走 wf_doc_understand 云轨（LaTeX+confidence），RapidOCR 兜底；confidence 落 parse_quality。
- **平台侧阻塞**：星辰文件上传 API 返回 404（/workflow/v1/files 及变体全探测 404/403），云轨暂不可用，自动降级本地 OCR。RapidOCR 对数学公式还原有失真（f'(1)→f(1)、x²→x~2），待队友确认星辰文件上传端点或工作流是否支持 base64 输入。

### 3.5 性能（任务7）
- 纯读接口 P95 < 155ms（filter 134 / heatmap 54 / due-queue 98 / practice-start 138 / group-recommend 94）。
- growth/panel 原 P50=2262ms（_polish_copy 每请求同步等 LLM）→ 加 Redis 缓存（24h）后 P50=76ms。
- weak-points / weekly-report P50 < 110ms（日缓存命中），缓存过期回填时偶发 2-3s，可接受。

## 5. 决策记录

| 决策点 | 结论 | 依据 |
|--------|------|------|
| 真 FSRS v4/v5 | ❌ 降级，保留启发式+展示修复 | 错题中位 1 题/人，复习事件 71 次 |
| BKT 四参数 | ❌ 降级为接口预留（app/services/bkt.py 已备），继续简单正确率 | 答题量中位 2 题/人（<20 阈值，49 用户仅 2 人达标） |
| 星辰 chat 流 | ❌ 停用，MiMo 为 chat 主力 | 丢上下文实测（3 条闭环 2 条坏） |
| 星辰文件上传服务 | ✅ 弃用，改工作流 data URI 直传图片 | 端点 404 下线，data URI 实测 code=0 |
| 前端 mock 默认值 | ✅ 反转：默认连真实后端，VITE_USE_MOCK=1 显式开 mock | 假数据误导用户 |
| TAL 重标速率 | 继续全量重标（速率 >5 题/分） | 实测约 15 题/分 |

## 6. 进行中事项
- ✅ **TAL 难度再平衡：已完成**（2026-08-16 复核）——4668/4668 全量打标，最终分布 easy 1912 / medium 2103 / hard 653（method 溯源在 annotate_meta.difficulty_relabel，断点续跑脚本 scripts/relabel_tal.py 幂等可重入）。
- ✅ **RAG 向量召回 unpack 报错：已修复**（2026-08-16，见 §3.10）——根因 `_vector_search` 的 `select(Chunk, KnowledgeDoc.title)` 少了 `distance` 列，三元解包 `(chunk, title, dist)` 对二元组解包抛 "not enough values to unpack"，被 except 吞掉 → 向量路恒空、退化 trgm 兜底。修复后 select 补 distance 列，实测 top1 即目标 chunk（cosine 距离≈0，raw_score=1.0）。
- 星辰平台侧待办：App A 五流 20373 授权/额度、solution_pregrade 20805 输出绑定、wf_socratic_chat 支持透传 system messages（修好后可恢复 chat 云轨）。
- smart_quiz 出题数量偶发少 1 题（5 次中 1 次，LLM 出题质量波动，非路由故障；代码已诚实降级 + 明示略去题数）。

## 6.1 迭代20 收尾轮（2026-08-16）

### 6.1.1 修复清单
- **RAG 向量召回 unpack 报错**（`app/kernel/rag.py::_vector_search`）：select 补 `distance` 列，修复三元解包。端到端验证（真实 chunk embedding 作查询向量）：召回 20 条，top1=目标 chunk，raw_score=1.0。
- **显式 spark_vl 引擎解析污染**（`app/domains/files/router.py::_dispatch_parse`）：`_parse_image_vision` 迭代19 已改为返回 `(text, confidence)` 二元组，但 `spark_vl` 分发分支仍按字符串处理（`if content:` 恒真 + 把元组当文本返回），显式 spark_vl 链路解析结果被污染。已改为解包 `content, _confidence`。
- **测试契约同步**（`tests/test_iter05_files.py`）：3 个用例（cloud_track / disabled / engine_fallback）随 `_parse_image_vision` 返回类型变更同步为 `(text, confidence)` 断言；删除已下线的 `upload_file` mock。

### 6.1.2 验证结果
- 全量 pytest：**806 passed, 1 skipped, 0 failed**（修复前 804 passed / 2 failed，均为 test_iter05_files.py 契约过期用例）。
- 端到端冒烟（真实后端 :8000 + 真实库）：登录 / 知识图谱 / 专练 5 题（bank5+ai0）/ 每日一题 / 全真模拟组卷 / 学情报告 / 管家周报 全部 code=0。
- 基础设施：Docker（Postgres 54329 / Redis 6379 / MinIO 9000）已拉起，迁移 head=m2_017，数据完整（题库 5727 / 知识点 53 / 用户 9013 / 错题 530 / chunks 10545）。

## 7. 质量红线自查
- ✅ 无硬编码假数据（所有接口连真实库）
- ✅ 未破坏已有功能（核心三接口实测 200；防幻觉评分/引导式对话/配图渲染/F13 可视化未动）
- ✅ SQL 全部参数化/ORM
- ✅ 单测 150+ 例全过；前端生产构建通过；管家闭环 14/15；拍照云轨全链路通过
- ✅ 敏感信息不入代码（星辰凭证仅 env/管理后台）