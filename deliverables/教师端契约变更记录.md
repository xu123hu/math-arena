# 教师端契约变更记录

> 双对话对齐锚点文件：后端优化对话与前端优化对话共用。
> 规则（与两份提示词的"契约锚点"条款一致）：
> - 后端修改/删除既有教师端接口的路径、字段、枚举、语义前，**必须先在下方追加条目**，禁止静默改契约；
> - 前端对后端接口有新增/调整期望时，在"期望方向=FE→BE"追加条目，由后端对话评估采纳；
> - 只增不破：新增字段可选、新增端点自由；修改/删除必须评估前端影响面；
> - 每条变更须有状态：proposed（已提出）→ accepted（对端已确认）→ shipped（已实现并联调通过）。

| 日期 | 方向 | 端点/契约 | 变更内容 | 前端影响面 | 状态 |
|---|---|---|---|---|---|
| 2026-09-01 | FE→BE | GET /teacher/classes/{id}/insights（及 today 内嵌洞察） | **洞察 kind 枚举 SSOT 确认**：前端 `TeacherClassesView.vue` INSIGHT_ACTIONS 使用 `review_backlog/low_mastery/submission_trend`，而前端 mock 为 `mastery_drop/queue_pressure`，两者零交集。请求后端给出权威 kind 枚举清单（含"错题聚类"固化后的新增 kind），前端按权威枚举适配映射表。【BE 答复 2026-09-01】权威枚举 = `error_cluster / submission_trend / low_mastery / review_backlog` 四类（来自 insights.py 候选链 `compute_class_insights`）；`mastery_drop/queue_pressure` 无后端实现，mock 应删除。与下方 BE→FE 第 1 条同源 | L5 行动跳转按钮当前在 mock 下全部落兜底；若后端枚举与 mock 同源则真实环境同样失效 | accepted |
| 2026-09-01 | FE→BE | POST /teacher/classes/{id}/classroom-session/question | **question_no 序号语义确认**：前端当前按 0 基索引发送（`api/teacher/classroom.ts` launchQuestion 原样透传视图 idx）。请求确认 0 基还是 1 基，双端统一。【BE 答复 2026-09-01】**0 基**：后端 `req.question_no` 定义为 `Field(default=0, ge=0, le=99)`，并按 `question_no % 题池长度` 直接索引题池（teacher/router.py:199、classroom_session.py:357）。阶段 3 持久化改造时维持 0 基不变 | L6 课堂发题对位错位风险 | accepted |
| 2026-09-01 | FE→BE | POST /teacher/lessons/{id}/adopt-suggestion（名称形状待后端定） | **"采纳建议"落库端点**（后端版提示词 §2.4 已列"本期新增①"，按前端期望补记）：入参=lesson_id+segment_id+suggestion_id，出参=更新后 artifact（走既有生命周期）。前端 L2"采纳建议"按钮将接线（当前纯本地状态无请求）。【BE 答复 2026-09-01】采纳该端点与命名形状：`POST /teacher/lessons/{lesson_id}/adopt-suggestion`，入参另加可选 `content`（采纳时可携带教师修改），出参=更新后 artifact；实现于阶段 2/3，届时本行转 shipped | L2 备课链采纳动作从假反应变真实落库 | accepted |
| 2026-09-01 | FE→BE | GET/POST /teacher/classes/{id}/classroom-session（start/question/close） | **会话查询语义确认**（后端版"本期新增②"）：前端在途代码已实现 fetch/start/launchQuestion/close 四端点（`api/teacher/classroom.ts:34-64`），但 mock 无路由、后端未实现。请求确认路径形状与会话查询返回字段（作答分布、AI 提醒）。【BE 答复 2026-09-01】后端**已实现**（随本次未提交 diff 落库）：GET `/teacher/classes/{class_id}/classroom-session`（state）、POST `…/start`、POST `…/question`、POST `…/close`，字段以 `test_m3_teacher_classroom.py` 断言为准。注意：当前为**进程内存 + TTL 2h** 实现，作答前分布为「待作答/合成」占位——阶段 3 持久化+诚实化后如有字段语义调整，将在此记录并通知 | L6 课堂链在 mock 与真后端均 404；前端在途 +308 行重写暂无法验收 | accepted |
| 2026-09-01 | FE→BE | GET /teacher/grading/queue（V1） | **V1 批改端点去留预通知**：前端计划将 today 深链与导航角标从 V1 queue 通路迁至 V2 workspace 通路（消除双数据通路）。迁移后若 V1 端点无其它消费者，请后端评估保留（兼容期）或下线；任何改动先在本记录登记。【BE 答复 2026-09-01】同意收敛到 V2 workspace 单通路；后端建议 **V1 queue 保留兼容期**（至少到前端迁移完成并回归通过），下线动作等前端申报迁移完成后由后端在此记录并执行 | L4 与 today 深链、TeacherNav 角标 | accepted |
| 2026-09-01 | BE→FE | GET /api/teacher/classes/{id}/insights | `kind` 枚举新增 `error_cluster`（跨作业错题集中失分聚类），并插入候选链首位——Top3 洞察的构成与排序随之变化（即上方 FE→BE 第 1 条的权威答复来源） | 前端按权威四枚举适配映射表 | shipped |
| 2026-09-01 | BE→FE | GET /api/teacher/today | `_next_lesson` 新增 6 个可选字段：`class_name / lesson_id / prep_completion / missing_items / duration_minutes / location`（备课完成度） | 附加型；`prep_completion` 可为 null（无教案时） | shipped |
| 2026-09-01 | BE→FE | GET /api/teacher/grading/workspace、GET grading/{id} | 题目上下文对象新增 `image` 字段（原题图，可空） | 附加型 | shipped |
| 2026-09-01 | BE→FE | GET /teacher/classes/{class_id}/classroom-session、POST …/start\|question\|close、GET /api/teacher/grading/insights | 5 个新增端点（课堂会话 + 批后讲评建议）。当前 classroom-session 为进程内存 + TTL 2h，阶段 3 将持久化；作答前分布非真实数据 | 新增端点，前端按上方 FE→BE 第 4 条的已确认形状接线 | shipped |
| 2026-09-01 | BE→FE | GET /api/files/{id}/content | `local:` 存储分支：`data.url` 由 MinIO presign 绝对 URL 改为相对路径 `/api/files/{id}/raw?token=…`，`expires_in` 可为 null（学生端 F2 断链修复，教师资源/错题图同源复用） | 消费方需兼容相对路径与 null 过期；配套新增 GET /api/files/{id}/raw | shipped |
