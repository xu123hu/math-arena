# 教师端优化提示词 · 后端版（调研驱动 · 链路验收）

> 上游模板：《AI编程通用提示词_调研驱动链路验收版_v1》。
> 本文件由平台代码架构师于 2026-09-01 基于对教师端前后端的全量代码盘点定制。
> 用法：全文粘贴给负责**后端**的编程智能体。按阶段顺序执行，**每个阶段结束停下来交证据、等确认**，禁止跳阶段、禁止合并汇报、禁止一口气做完。
> 注：文中 file:line 证据来自 2026-09-01 盘点，行号可能漂移，以符号名为准。

---

## 0. 项目信息（必读）

- **项目名称**：智学数研（zhixue-shuyan）· 教师端后端
- **一句话定位**：高中数学教师在"备课 → 组卷布置 → 批改 → 学情讲评"的教学循环中，用它把 AI 组卷、AI 批改建议、班级学情洞察变成可靠落地的日常工作流。
- **目标用户**：普通高中数学教师（带 1~4 个班、约 40~200 名学生；日常时间大量消耗在批改与学情统计上；对"AI 出题文不对题、AI 批改不可信"高度敏感）。
- **技术栈与代码位置**：
  - 仓库 `D:\math-arena`，分支 `feat/backend-m1`；后端服务 `services/api`，端口 8000，全局前缀 `/api`，教师域 `/api/teacher/*`；
  - FastAPI + SQLAlchemy + Alembic + PostgreSQL(pgvector) + Redis；分层铁律：`gateway(角色路由) → domains(业务) → kernel/skills/providers/butler`，`app/main.py` 只做装配；
  - 测试：`cd services/api && pytest`（conftest 强制独立库 test_math_arena@localhost:54329，且 `M3_ENABLE_TEACHER=true`）；依赖 `deploy/docker-compose.yml` 的 postgres/redis；
  - 配套前端在 `D:\frontend`（Vue3+Vite+Pinia，**另一个对话正在并行优化**，见最高原则第 5 条"契约锚点"）。
- **既有资产（优化项目必填 · 候选盘点清单，不是"不可重写"封条）**：
  以下是架构师盘点到的候选骨干与资产（含证据位置），**是否保留由你分析后判定**：请在阶段 0 通读代码后逐项评估，产出《既有资产评估表》，每项三选一——**保留**（确属骨干，写明理由）/ **改造**（实现有债，列入修改清单）/ **重写**（确属劣质实现，附证据与替代方案），随阶段 0 汇报提交待我确认。评估结论生效后："保留"项享受最小侵入保护；"改造/重写"项按你的方案执行，不视为违反红线 4。
  1. 高考真题库（901 道真题 / 学生可见 4,767 题，`models/question_bank.py:30`；导入脚本 `scripts/import_gaokao_mm.py`、`import_cmm_math.py`）；
  2. 出题五闸 + 审题纪律 + 黑盒复核（`skills/smart_quiz/main.py:126/493/764`）——目前只服务学生端；
  3. 双师课堂引擎（`domains/classroom/stage_router.py` + openmaic_adapter + math_verifier + RAG grounding；迁移 om1/om3；failed 不伪装 ready 纪律在 `stage_router.py:2031/2105`）；
  4. SSE 推流 + 并发闸（`gateway/agent_router.py:83-86`，全局 20/每用户 3）；
  5. 文件上传解析管线（`domains/files/router.py:317` 起：pymupdf / RapidOCR / MiMo-OCR→LaTeX / pandoc / local 流）；
  6. RAG 三路混合检索 + 拒答闸（`kernel/rag.py:101-314`，BGE-M3 阈值已实测校准）；
  7. ModelRouter 星火→DeepSeek 降级 + 熔断 + ai_calls 审计（`providers/router.py:88-236`）；
  8. Butler Runtime / PolicyGate / 教师 19 工具（`butler/*`、`teacher/registry.py:301-330`，WRITE 需确认 + 幂等）；
  9. 批改幂等 / 审计纪律（pg advisory lock + binding，`teacher/grading.py:265-317`，已过专项测试）；
  10. 越权测试基线（`tests/test_m3_teacher_scope.py`，11 个越权用例）；
  11. 迁移链单线 head：`om5_error_record_origin`（注意 om1→om3 串接顺序与文件名直觉不同，新迁移须挂在当前 head 上）；
  12. 架构师初判提示（仅供参考，仍以你的评估为准）：第 9 项（批改幂等/审计纪律，`grading.py:265-317`）与第 10 项（越权测试基线，11 个越权用例）经过专项测试验证、属纪律性资产，动它们之前需特别谨慎并留证。
- **本期范围**：
  - **做**：本提示词 §4 候选任务（经你验证后执行）——主观题 AI 批改、教案 LLM 生成路径、课堂会话持久化与统计诚实化、未提交改动落库、性能债（N+1/分页/索引）、资源管线接入 files/RAG、Butler 真执行，以及你分析中新发现并挂上链路卡的问题；
  - **不做**（默认边界，经阶段 0 论证 + 我确认后可突破）：学生端接口改造、前端页面实现（归前端对话）、新增独立微服务；无靶的大规模推倒以红线 4 为界。

### 0.1 当前工作区风险（开工第一步必须处理）

`git status` 显示 29 文件 **+3340/-163 未提交**（`stage_router.py` +2222、`teacher/router.py` +107、`grading.py` +125、`insights.py` +137、`today.py` +66 等），且关键新文件 untracked（v7_engine、openmaic_adapter、om3~om5 迁移、12 个课堂测试）。**禁止在这批流沙改动之上直接叠新功能**。第一步动作：跑通测试基线 → 分主题审查这批 diff（主题≈"调研版体验补齐"：讲评建议 / 错题聚类 / 备课完成度 / 课堂会话）→ 分组 commit 落库 → 证据留档。

---

## 最高原则（在模板 4 条之上，本项目追加 2 条）

1. **先调研、后动工**——不允许凭想象写需求、写接口、写内容；
2. **链路即验收**——第 2 节的链路卡就是最终验收标准，未过链路验收的交付视为未完成；
3. **最小侵入**（模板原文）——保留既有内核与接口契约，只做增量，禁止另起炉灶；"内核"的范围以你阶段 0 的《既有资产评估表》结论为准，不预先封死；
4. **证据交付**——一切结论必须有证据：调研给来源链接/截图，代码给运行时证据（curl/SQL/pytest 输出），不接受"代码看着对"；
5. **契约锚点（双对话对齐纪律）**：前端对话正在并行优化 `D:\frontend`。教师端接口契约以本文档 §2.3 清单为双端唯一锚点。**只增不破**：新增字段可选、新增端点自由；修改/删除既有路径或字段前，必须先写入 `D:\math-arena\deliverables\教师端契约变更记录.md`（格式：日期 / 端点 / 变更内容 / 前端影响面），禁止静默改契约；
6. **骨架共享**：§1 的链路清单与画像底稿是架构师预置的**参考底稿**（同一份底稿已发给前端对话），不是结论。你应在阶段 0/1 用自己的分析验证、修订甚至推翻其中的内容，但修订必须：a) 附证据与理由；b) 在阶段汇报"偏差"栏申报，经我确认后由我同步给前端对话，保证双端一致。

---

## 1. 阶段 0：调研核验与补全（优化项目：先盘点已有调研，再补缺口，禁止从零重做）

### 1.1 已有调研资产（先通读，作为调研底稿）

1. `D:\math-arena\教师端体验诊断报告_v2.md`（2026-08-22，421 行）——已诊断：P-01 备课无课题输入、A-01/A-02 出题文不对题、G-01 批改缺题干（本次未提交 diff 已部分修复）、B-01 Butler 只复述不执行、§3.1 原始字段暴露（`insights.py:28-48`、`grading.py:162-190`，修复中）；
2. `D:\math-arena\deliverables\通宵优化修复总报告-2026-09-01.md`；
3. 前端 `D:\frontend\research-repos\`：TeacherBuddy / Paper LMS / Nellavio 三个 MIT 克隆基准（前端已用作页面参照）；
4. 教师画像底稿（必须用调研证据修订后采用，禁止原样拍板）：
   - **画像 A · 批改减负型**：王老师，38 岁，县城高中数学教师，带 2 个班约 110 人；每天晚自习在办公室 PC 批改作业 2~3 小时；第一分钟打开产品想看"今天要批多少份、哪些学生需要关注"；最怕 AI 批改不可信（错批还要自己重批）、数据丢失。对应链路 L4/L5。
   - **画像 B · 出题质量焦虑型**：李老师，26 岁，入职第二年新教师；想用 AI 组卷节省备课时间，但被"文不对题、超纲、重复题"伤过（对应诊断 A-01/A-02）；第一分钟想"按课题出一套可信的练习"；最怕题目质量失控、被学生问倒。对应链路 L2/L3。

### 1.2 需补全的调研（产出《教师端后端调研报告》）

1. **竞品**：同类 ≥5 + 间接 ≥3，方向建议（可换）：智学网 / 好分数（批改+学情报表）、组卷网 / 学科网 / 菁优网（组卷题库）、希沃白板 / 班级优化大师（课堂互动）、班级小管家 / 钉钉家校（作业发布）。每个输出：目标用户、核心功能、**真实界面截图**、交互流程、定价、用户差评来源；
2. **画像卡**：按底稿核验修订 2~3 张，每张回答"第一分钟想干什么 / 最怕什么"；
3. **痛点—功能映射表 + 砍单清单**：本期待办（§3）逐条挂到真实痛点，挂不上的移入"本期不做"；
4. **技术基准**：clone 2~3 个同领域优秀开源项目到 `D:\math-arena\reference-repos\`（加入 .gitignore）。候选方向：`zhanymkanov/fastapi-best-practices`（项目结构/服务层模式）、`tiangolo/full-stack-fastapi-template`（分页/鉴权/测试组织）、外加一个批改/题库同域项目（调研后自选并说明理由）。逐个输出《技术基准分析》（目录划分 / 数据流 / API 风格 / 鉴权与错误处理 / 测试组织），每项工程决策三选一：直接复用 ＞ 改造复用 ＞ 自研创新，并说明理由。**借鉴纪律：只借鉴架构思想与模式，禁止整套硬搬**。

**阶段 0 验收**：调研报告（竞品表 + 画像卡 + 痛点映射 + 砍单 + 技术基准分析 + **既有资产评估表**），附来源链接与截图。我确认后才进入阶段 1。

---

## 2. 阶段 1：教师端用户操作链路（骨架已预置，你需逐卡确认、补异常降级、给出断头路清零方案）

### 2.1 链路清单（8 条）

| 编号 | 链路 | 入口 | 状态 |
|---|---|---|---|
| L1 | 今日工作台 | `/teacher/today` | 可用，有性能债 |
| L2 | 备课（教案/课件） | `/teacher/prep` | **模板空壳，痛点最大** |
| L3 | 组卷布置 | `/teacher/assign` | 可用，质量待验证 |
| L4 | 批改工作台 | `/teacher/grading` | V2 可用，主观题 AI 缺失 |
| L5 | 班级学情→讲评 | `/teacher/classes` | 可用，错题聚类刚加 |
| L6 | 课堂互动 | `/teacher/classroom` | **半成品：内存会话+合成统计** |
| L7 | 资源库 | `/teacher/resources` | 可用，理解管线低配 |
| L8 | Butler 管家 | 全局侧栏 | **空壳：只读不执行** |

### 2.2 黄金路径声明（资源优先倾斜）

**L3 → L4 → L5 作业闭环**：出题布置 → AI 批改建议 → 人工确认 → 班级学情/错题聚类 → 讲评建议。这是教师减负价值最大的一条链，本期所有 P0 任务必须能挂到这条链上。

### 2.3 链路卡 + 接口契约锚点

**L4 批改链（黄金路径核心）**：
- 步骤：学生提交 → GET `/api/teacher/grading/queue|workspace` → POST `/grading/{id}/suggest`（AI 建议）→ 人工 review/confirm / batch-confirm → 错题与掌握度落库；
- 数据：`submission_items` 双轨字段（suggested_score / suggestion_status / needs_review / teacher_final_score）、`teacher_actions` 幂等台账；
- 现状断点：主观题（非 choice/judge）直接 manual_review 无 AI（`grading.py:456-461`）；capability 本地兜底硬编码 0 分草稿（`capability_gateway.py:226-227`）产生误导；workspace 每题循环查询 N+1（`grading.py:753-757`）；suggestion 用 JSONB 字符串匹配全 owner 扫描（`grading.py:518-545`）；
- 异常与降级：AI 不可用 → 明示 manual_review 状态（禁止静默 0 分）；幂等重放走 `teacher_actions`。

**L3 组卷布置链**：
- 步骤：POST `/quizzes/generate`（题库两阶段检索 question_supply）→ artifacts confirm → POST `/assignments` → POST `/assignments/{id}/publish`；
- 数据：quizzes / quiz_items / assignments（source_artifact_id 关联）/ ClassMember 范围；
- 现状断点：检索质量（A-01/A-02 文不对题根因）；list_assignments 无分页（`assessment.py:500-508`）。

**L5 学情讲评链**：
- 步骤：GET `/classes/{id}/insights`（4 类确定性洞察 + 新增 `_error_cluster` 跨作业共同错题）→ 行动跳批改/备课；
- 断点：today 对每班循环全量计算（`today.py:209-216`）；新增洞察未提交。

**L2 备课链**：
- 步骤：POST `/lessons/adapt` → artifacts confirm → POST `/lessons/{id}/slides` → GET docx/pptx 下载；
- 断点：星辰未配置时恒降级本地模板（`lessons.py:63-79`、`capability_gateway.py:384-387`）= 诊断 P-02/P-03"空壳时间线"根因；"采纳建议"无落库端点（前端当前纯本地，需本期新增契约）。

**L1 今日工作台**：GET `/teacher/today` 聚合待批/洞察/备课完成度；断点：N+1。
**L6 课堂链**：POST `/classes/{id}/classroom-session/start|question|close`；断点：`_SESSIONS` 进程内存（`classroom_session.py:195`，重启即丢、多 worker 失效）、题池写死 3 题（`:37-62`）、作答分布确定性伪造（`:146-176` 与模块自述冲突）。
**L7 资源链**：upload → preprocess/understand → publish → question-candidates/approve；断点：理解管线是 500 字符切片+截断摘要（`resources.py:240-279`），未接 files/RAG 既有管线；资源实体复用 TeacherTask 行（`resources.py:391-401`）抽象泄漏。
**L8 Butler 链**：POST `/butler/chat`、GET/POST `/capabilities`；断点：PolicyGate 只放行 READ/LEARNING_ACTION（`router.py:838`），聊天无法产生 artifact（B-01 根因）；Runtime/Registry 每请求重建（`router.py:835/906/979`）。

### 2.4 接口契约稳定性标注（§2.3 之外的全部教师端点同样适用）

> 契约稳定约束的是**接口形状**（路径/字段/语义），不是实现质量——服务层、SQL、内部结构等实现层面你拥有完全修改权（按《既有资产评估表》结论执行）。另：§2.3 各链路卡中的"现状断点"是架构师盘点到的**候选问题**（含证据位置），须在阶段 1 验证后方可作为靶子，发现描述有误请在汇报中纠正。

- **保持稳定**（前端已依赖，禁止改路径/字段语义）：today、insights、video-insights、classroom-mode、grading queue/workspace/suggest/review/confirm/batch-confirm/insights/file、quizzes/generate、assignments 全套、lessons/adapt+slides+download、artifacts confirm/publish、resources 全套、butler/chat、capabilities、tasks/{id} 轮询；
- **本期增强**（向后兼容加字段）：grading suggest（主观题 AI 化）、lessons/adapt（LLM 路径）、insights（错题聚类固化）、today（性能不变形状不变）；
- **本期新增**（走契约记录，供前端接线）：① 教案"采纳建议"落库端点；② classroom-session 持久化后的会话查询语义；③（调研后可选）讲评报告生成端点。

**阶段 1 验收**：链路卡全集逐条确认 + 每步异常降级补全 + 断头路清零方案。我确认后才进入阶段 2。

---

## 3. 阶段 2：链路转化为验收标准（双轨）

1. **功能轨**：每条链路逐步骤写"输入→输出 + 接口/数据断言 + 异常路径处理"，验证方式注明（curl / SQL / pytest / 页面截图）。示例：主观题 suggest 后 `suggestion_status` 必须为 ai_suggested 或 manual_review 之一且附依据文本，禁止 0 分兜底；
2. **体验轨**：接口返回内容必须"像人做的"——洞察文案含真实学生/题目上下文，禁止原始字段直出（诊断 §3.1 遗留）；演示数据必须是真实班级/真实题目，禁止占位符；
3. **内容资产清单**：演示用真实班级、真实学生、真实作答数据的灌入方案（谁产出、什么标准）；
4. **AI 产物基准库**：教师端 AI 生成物 = 教案 docx、课件 pptx、讲评/学情报告、AI 题目。前三类各搜集 **≥5 份市面优秀真实样本**（优秀公开课教案/课件/真实学情分析报告）下载到 `D:\math-arena\reference-repos\ai-baseline\`，逐份标注"优秀在哪"；AI 题目以真题库 901 题为风格基准。后续验收 = 生成物与基准并排对比；
5. （后端无 UI 施工图，但阶段 4 须为前端黄金路径走查提供真实可用数据。）

**阶段 2 验收**：验收用例表（链路 × 双轨）+ 内容资产清单 + AI 产物基准库。我确认后才进入阶段 3。

---

## 4. 阶段 3：构建实施（顺序：黄金路径 → 其余链路；每完成一条立即端到端留证）

> 本节 P0/P1/P2 是架构师基于代码盘点预判的**候选靶子**（含证据位置），不是既定结论。开工前必须逐条验证：确认属实的按此执行；判断有误或另有根因的，在汇报中推翻并给出你的方案——禁止不经验证照单执行。

### 4.1 P0 任务清单（全部挂在链路上）

1. **未提交 diff 落库**（§0.1）：测试基线 → 分主题 commit → 运行证据（挂所有链路的前置条件）；
2. **L4 主观题 AI 批改**：suggest_grade 接 ModelRouter（或星辰 wf_solution_pregrade），输出分数+评分依据+置信度；移除 `capability_gateway.py:226-227` 的 0 分兜底；LLM 不可用 → 明示 manual_review；写行为测试（含降级路径）；
3. **L2 教案 LLM 路径**：星辰不可用时降级 ModelRouter 真生成（结构化教案），淘汰恒模板降级；配合前端补"课题输入"契约（P-01）；生成结果与 AI 产物基准库并排对比；
4. **L6 课堂会话持久化与诚实化**：`_SESSIONS` → DB 表 + Alembic 迁移（挂当前 head）；题源改为已确认 artifact/题库检索；作答前不伪造分布——改为"待作答"语义，作答后给真实统计；
5. **L8 Butler 真执行（B-01）**：PolicyGate 放开受控 WRITE（保留确认 + 幂等纪律），使对话能落 artifact；Registry/Runtime 加进程级缓存。

### 4.2 P1 任务清单

6. **性能债**：workspace N+1（`grading.py:753-757/657-664`）、today 每班循环（`today.py:209-216`）、suggestion JSONB 全扫（`grading.py:518-545`，建索引或结构化列）、list_assignments/list_lessons/grading_queue 分页、resources limit 100 硬编码（`resources.py:399`）；
7. **L7 资源管线接入 files/RAG**：preprocess/understand 复用 `domains/files/router.py` 解析管线与 `kernel/rag.py` 检索，替换低配切片；
8. **契约信封收敛**：新端点统一 `_ok` 信封；既有端点不动（最小侵入）；`/capabilities` 内联 140 行下沉服务层（`router.py:1008-1083`）。

### 4.3 P2 任务清单（P0/P1 验收有余量才做）

9. 三套"课堂"概念（ClassroomMode 表 / 内存 session / classroom_sessions）语义收敛——先文档化不动表；
10. `stage_router.py`（2479 行）拆分——仅小步、有测试护航，禁止大规模重构。

### 4.4 实施纪律

- **单一内核**：同类能力一套实现（出题只走 question_supply，批改只走 grading 服务）；
- **设计必引基准**：新模块动手前声明"参照 reference-repos 中 X 项目的 Y 结构"；
- **增量纪律**：改动前列出将动的文件与理由；
- **真实内容**：演示/文案数据人审后灌入，AI 只做管道；
- **优雅降级**：外部依赖（星辰/星火/DeepSeek/OCR）不可用必须明示降级状态字段，禁止静默失败；
- 每完成一条链路：curl + SQL + pytest 证据落 `deliverables/phase3-evidence/`（新建），再开下一条。

---

## 5. 阶段 4：链路验收（最终门禁）

- 按阶段 2 用例表逐条走查，每条链路输出 通过/不通过 + 证据；
- **AI 生成物专项**：教案/课件/报告各随机抽 ≥3 份，与基准库样本并排截图对比，输出《产物对比验收表》（结构/排版/内容密度/专业感逐项打分）；
- 黄金路径（L3→L4→L5）从"出题布置"到"讲评建议"完整走一遍，全程 curl/SQL/日志留证；
- 输出《验收报告》：用例通过率 + 证据索引 + 遗留问题清单（含严重级别）。

---

## 6. 红线（任何一条违反即返工）

1. 没有阶段 0 调研报告，禁止写任何代码；
2. 禁止凭空编造需求、接口、内容、数据——需求来自调研，接口形状来自链路卡，内容来自真实素材；
2b. 禁止 AI 生成物"裸奔"交付——教案/课件/报告未建基准库、未并排对比验收前，禁止视为完成；
2c. 禁止凭空自研——工程实现必须参照 reference-repos 已克隆项目，有成熟模式不许自造劣质轮子；
3. 禁止无靶迭代——一切修改必须对应某条链路验收用例的未通过项；
4. 禁止大规模推倒重构——禁止无靶全量重写；经《既有资产评估表》判定为"改造/重写"的模块允许修改与重写，前提是：对应某条链路验收用例的未通过项、小步提交、测试护航、汇报申报；
5. 禁止只交代码不交证据——每个改动附运行时验证（curl/SQL/pytest 输出）；
6. 禁止功能堆叠——新功能必须挂到某条链路卡；
7. 每阶段结束停下来等我验收，禁止自作主张连续推进；
8. **契约红线**：修改/删除既有教师端接口契约前未写入《教师端契约变更记录》即返工。

---

## 7. 汇报格式（每阶段结束按此提交）

```
【阶段 X 完成汇报 · 后端】
1. 产出物清单（文档/文件路径/接口/迁移/数据）
2. 验收证据（pytest 输出 / curl 响应 / SQL 查询结果 / 截图编号）
3. 发现的问题与取舍说明
4. 与本提示词要求的偏差（如有，说明原因；含对链路骨架/画像底稿的任何修订）
5. 契约变更记录（如动过接口，附《教师端契约变更记录》新增条目）
6. 下一阶段开工前需要我确认的事项
```
