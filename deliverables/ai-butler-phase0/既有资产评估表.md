# 既有资产评估表 · AI 管家悬浮球 + 后台任务中心（优化项目必填）

> 日期：2026-09-01 ｜ 结论先行：**内核与契约全部保留，本期只做「任务中心 + 通知 + 3 个管家工具 + 双端悬浮球入口」的增量**；四套后台任务模式收敛为一套，不推倒任何业务表。

## 1. 后端资产

| 资产 | 位置 | 现状 | 本期处置 |
|---|---|---|---|
| Butler v2 Kernel | `app/butler/`（runtime/policy/executor/registry/contracts/composer/context/model_adapter） | 完整 Agent 编排框架：Context→Plan→Policy→Execute→Compose 固定管线；agent_runs/agent_steps/tool_invocations 三表记账；预算控制（模型≤3/工具≤5/超时≤20s）；幂等 client_request_id | **直接复用，零改动内核**。学生端补场景登记（`_v2_migrated_scenes()` 空集是唯一闸门）；新增 3 个领域工具进 registry |
| Butler v1（事件总线/skills） | `app/butler/event_bus.py`、`orchestrator.py`、`skills.py` | 事件驱动文案层：quiz_judge 判分后自动触发（student_router.py:364-383 唯一实接点）；LLM 仅润色 + 兜底 | 保留；管家悬浮球与 v1 并存（v1=主动关怀，v2=执行代理） |
| 教师管家实跑通道 | `domains/teacher/router.py:826-909`（19 工具 registry + POST /teacher/butler/chat） | 已上线（Policy 放行 READ+LEARNING_ACTION） | 悬浮球教师端接同一端点；备课/组卷工具按需补 WRITE 放行 |
| 管家工具清单 | `app/butler/tools.py`（9 本地）+ `workflow_tools.py`（7 星辰）+ `domains/teacher/registry.py`（19） | 已有出题（practice.variants.create / xingchen.smart_quiz）、课程预处理、错题、路由跳转 | **缺 3 个关键工具**：①智能出题（走练题中心同一内核含五闸）②双师课堂会话生成 ③引导式解题转接（含图片）。本期补齐 |
| 课堂生成后台范式 | `domains/classroom/stage_router.py`（status=generating 落库 → `create_task(_run_generation)` → SSE events → stale>90s auto-resume L2340/2916） | 仓库内唯一「断连不受影响 + 自愈」完整实现 | **推广为通用 TaskRunner 蓝本**（不动 classroom 本身） |
| teacher_tasks 任务表 | `models/teacher.py:147-171`（queued/running/succeeded/failed/cancelled + progress/artifact/result/payload） | 唯一通用状态机任务表；但教师专属写入、当前多同步完成即标 succeeded | 保留不动；新 `tasks` 表字段与其对齐，教师域二期渐进迁移 |
| 练题中心出题内核 | `gateway/student_router.py:1052` practice/start（题库优先→AI 补缺口→五闸，Semaphore(3)） | 同步在请求内完成（守页面根因之一） | **内核不重写**：抽为可后台调用的服务函数，practice/start 与管家工具同参数化调用（单一内核纪律） |
| 星火主通道 | `providers/spark.py` + `router.py` 三层回退 | **本轮已接 Spark-X2.5-4B**（maas-api MaaS，44 测试通过 + 运行时取证 ALL CHECKS PASSED） | 已完成，为管家/后台生成供模型 |
| 未提交工作区 | config/stage_router/student_router 等 14 个 M 文件 + om6 迁移 | 与教师端「未提交 3535 行」台账对应 | 不触碰、不回滚；新改动叠加其上 |

## 2. 前端资产（D:\frontend，Vue3 + Pinia）

| 资产 | 位置 | 现状 | 本期处置 |
|---|---|---|---|
| 教师端 ButlerPanel | `src/components/teacher/ButlerPanel.vue` + TeacherLayout.vue:66 挂载 + ⌘K（L131-136） | 已上线的助手对话面板（非流式） | 升级为悬浮球双形态底座（Notion 式悬浮↔侧栏），加任务态卡片 |
| useAsyncTeacherTask | `src/composables/useAsyncTeacherTask.ts`（1200ms 轮询/60s 超时/取消） | **孤儿组件**（无页面引用）——现成轮询底座 | 复用改造为通用任务轮询 composable |
| AsyncTaskProgress | `src/components/teacher/AsyncTaskProgress.vue` | 孤儿纯展示组件 | 并入任务中心 UI |
| 保存状态机范式 | useClassroomProgress/useClassroomNotes（保存中/已保存/失败待重试+重试按钮） | 已落地（student-ai-upgrade P3） | 任务/通知 UI 沿用同一三态语言 |
| 学生端 V4Layout | `src/components/V4Layout.vue` | 无浮层挂载点（右栏常驻） | 顶层新增悬浮球节点（独立组件，不侵入现有三栏） |
| sse.js | `src/api/sse.js`（streamChat/streamClassroomEvents，含鉴权头/401 处理） | 稳定 | 任务事件若走 SSE 直接复用；一期轮询则用 client.js |
| 双师课堂进度 | DualView.vue:847-984（SSE 直播+轮询兜底+入场动画） | 已上线 | 管家建课堂后跳 /dual/{id} 直接复用整套进度 UI |
| 通知位 | TeacherLayout 顶栏铃铛（todayStore 聚合）；学生端无 | 教师有雏形、学生缺位 | 双端统一通知红点组件 + 任务中心入口 |

## 3. 方案文档资产

| 文档 | 复用点 |
|---|---|
| `product-strategy/ai-butler-restructure-2026-08-14.md`（843 行 v3.0 总方案） | ButlerAction Queue 持久化/重试/幂等设计、反骚扰纪律（每日≤3、8h 去重、考试静默、可关）、管家人格「小婷」四红线、P0-P2 需求表——**本期任务通知规则直接继承** |
| `butler-restructure/db-migration.sql` | learning_events/ai_recommendations 表范式（幂等键+状态+retry_count） |
| `学生端场景清单.md` D1-D10 | 错题/解题衔接面缺陷清单 = 管家「解题转接」工具的验收靶点（D2 错题→引导重解无入口，管家正是新入口） |
| `教师端体验诊断报告_v2.md` | 「教学助手必须是执行代理」§5.4 = 本期管家定位原始声明；B-03/P-04 = 任务态 UI 修复靶点 |

## 4. 欠账清单（既有方案遗留，本期收口）

1. **APScheduler 定时 worker**（butler-restructure overview 待办）→ 本期不做定时，但通用任务表为其留 kind/cron 字段。
2. **异步收尾缺失**（D5 pending_review 无补录）→ 任务化后判分回填挂任务中心，失败可见可重跑。
3. **事务后异步 emit 语义**（方案待确认 #11）→ 随 TaskRunner 事件化一并裁定（任务表写入与业务事务同库同 commit，规避）。
4. **学生端 v2 切流**（`_v2_migrated_scenes()` 空集）→ 本期登记 student.chat。

## 5. 红线对照自检

- 不推倒重构：四套后台模式**收敛不删除**（classroom/courses/files 现有机制保留，新任务只走通用表）；
- 单一内核：出题/课堂/解题三能力全部参数化调用既有内核，管家不另写生成逻辑；
- 接口契约：新增端点全部走 gateway 信封（code==0/ApiError），不破坏现有契约；
- 密钥纪律：本期唯一新增凭证为 Spark MaaS key，已入 .env（gitignored），代码库零硬编码。
