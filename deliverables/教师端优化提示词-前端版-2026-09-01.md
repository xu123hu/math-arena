# 教师端优化提示词 · 前端版（调研驱动 · 链路验收）

> 上游模板：《AI编程通用提示词_调研驱动链路验收版_v1》。
> 本文件由平台代码架构师于 2026-09-01 基于对教师端前后端的全量代码盘点定制。
> 用法：全文粘贴给负责**前端**的编程智能体。按阶段顺序执行，**每个阶段结束停下来交证据、等确认**，禁止跳阶段、禁止合并汇报、禁止一口气做完。
> 注：文中 file:line 证据来自 2026-09-01 盘点，行号可能漂移，以符号名为准。

---

## 0. 项目信息（必读）

- **项目名称**：智学数研（zhixue-shuyan）· 教师端前端
- **一句话定位**：高中数学教师在"备课 → 组卷布置 → 批改 → 学情讲评"的教学循环中，通过 8 个页面使用 AI 组卷、AI 批改工作台与班级学情洞察；前端负责把后端能力变成可信、可点、有真实反馈的操作界面。
- **目标用户**：普通高中数学教师（带 1~4 个班、约 40~200 名学生；在办公室 PC 上备课批改，手机看通知；对"假数据、点了没反应、AI 内容不可信"零容忍）。
- **技术栈与代码位置**：
  - 仓库 `D:\frontend`（独立 git 仓库，项目名 zhixue-shuyan-v4）；Vue 3.5 + Vite 5 + vue-router 4 + Pinia 2，JS/TS 混合（入口 `src/main.js`、路由与 `src/api/client.js` 为 JS；教师端 API/stores/类型为 TS，`src/api/teacher/*.ts`、`src/stores/teacher/*.ts`、`src/types/teacher.ts`）；
  - 分层：`src/api`（薄请求层，信封 `{code,message,data,request_id}`，401 刷新重试在 `client.js:78-88`）→ `src/stores`（pinia，教师端 8 个 store，带 AbortController 与幂等 tracker）→ `src/pages/teacher`（7 页）+ `src/pages/teacher-v2`（批改 V2）+ `src/features/teacher-grading-v2`；
  - dev 经 vite proxy 到 `VITE_API_PROXY_TARGET || 127.0.0.1:8000`（`vite.config.js:9,33-35`）；测试：`vitest`（`test/teacher/` 22 个文件）+ `playwright`（`e2e/m3-teacher.spec.ts`、`e2e/teacher-grading-v2.spec.ts`，跑在 VITE_USE_MOCK=1）+ `npm run typecheck`；
  - 配套后端在 `D:\math-arena\services/api`（**另一个对话正在并行优化**，见最高原则第 5 条"契约锚点"）。
- **既有资产（优化项目必填 · 候选盘点清单，不是"不可重写"封条）**：
  以下是架构师盘点到的候选资产（含证据位置），**是否保留由你分析后判定**：请在阶段 0 通读代码后逐项评估，产出《既有资产评估表》，每项三选一——**保留**（确属骨干，写明理由）/ **改造**（实现有债，列入修改清单）/ **重写**（确属劣质实现，附证据与替代方案），随阶段 0 汇报提交待我确认。评估结论生效后："保留"项享受最小侵入保护；"改造/重写"项按你的方案执行，不视为违反红线 4。
  1. 教师 8 页路由骨架（`src/router/index.js:36-43`）：today / prep / assign / grading(V2) / classroom / classes / resources / profile；
  2. 批改工作台 V2（`pages/teacher-v2/TeacherGradingV2View.vue` + `features/teacher-grading-v2/`：路由 query 同步、乐观锁 version、幂等键、文件失败恢复）——架构师初判工程质量较高，是否作为全站标杆由你验证后决定；
  3. 学生端可复用件：SSE 聊天栈（`composables/useChat.js` + `api/sse.js` + `components/chat/MessageBubble.vue`）、`useFileUpload.js`（压缩+presigned，已被课堂上传复用）、Markdown/KaTeX 渲染管线 `MarkdownView.vue`；
  4. 教师 stores 的取消与幂等模式（`stores/teacher/*`）；
  5. UI 基准库 `D:\frontend\research-repos\`（TeacherBuddy / Paper LMS / Nellavio，MIT 克隆，各页注释已声明参照来源）；
  6. 测试资产：22 个教师 vitest 文件 + 2 个教师 e2e spec；
  7. 样式体系：全局 `styles/teacher.css`（t-* token）+ 页面 scoped CSS。
- **本期范围**：
  - **做**：本提示词 §4 候选任务（经你验证后执行）——批改 V1/V2 双轨收敛、假数据清零、Prep 交互升级、死配置清理、API 调用收敛、渲染器共享内核、mock/e2e 补盲区、黄金路径 UI 对照施工图，以及你分析中新发现并挂上链路卡的问题；
  - **不做**（默认边界，经阶段 0 论证 + 我确认后可突破）：学生端页面改造、后端接口实现（归后端对话）、引入新 UI 组件库；无靶的大规模推倒以红线 4 为界。

---

## 最高原则（在模板 4 条之上，本项目追加 2 条）

1. **先调研、后动工**——不允许凭想象写需求、写界面、写内容；
2. **链路即验收**——第 2 节的链路卡就是最终验收标准，未过链路验收的交付视为未完成；
3. **最小侵入**（模板原文）——保留既有内核与接口契约，只做增量，禁止另起炉灶；"内核"的范围以你阶段 0 的《既有资产评估表》结论为准，不预先封死；
4. **证据交付**——一切结论必须有证据：调研给来源链接/截图，改动给运行时证据（页面截图/vitest/e2e 输出），不接受"代码看着对"；
5. **契约锚点（双对话对齐纪律）**：后端对话正在并行优化 `D:\math-arena`。教师端接口契约以本文档 §2.3 清单为双端唯一锚点（与后端提示词同源）。前端对契约的期望变更（新增端点/字段），写入 `D:\math-arena\deliverables\教师端契约变更记录.md` 并在阶段汇报中列出；后端已记录的变更按记录适配。**禁止在 mock 中私自"实现"不存在的后端契约当作完成**——mock 只用于 e2e 稳定性，交付判定以真实后端联调为准；
6. **骨架共享**：§1 的链路清单与画像底稿是架构师预置的**参考底稿**（同一份底稿已发给后端对话），不是结论。你应在阶段 0/1 用自己的分析验证、修订甚至推翻其中的内容，但修订必须：a) 附证据与理由；b) 在阶段汇报"偏差"栏申报，经我确认后由我同步给后端对话，保证双端一致。

---

## 1. 阶段 0：调研核验与补全（优化项目：先盘点已有调研，再补缺口，禁止从零重做）

### 1.1 已有调研资产（先通读，作为调研底稿）

1. `D:\math-arena\教师端体验诊断报告_v2.md`（2026-08-22，421 行）——P-01 备课无课题输入、A-01/A-02 出题文不对题、G-01 批改缺题干（后端修复中）、B-01 Butler 只复述、§3.1 原始字段暴露；
2. 前端参照体系：`research-repos/`（TeacherBuddy / Paper LMS / Nellavio）、`teacher-design-showcase.design/`、`docs/teacher-v2|teacher-refactor`、`artifacts/teacher-v2|teacher-refactor`（历史验收截图）、各页头注释的参照声明（Assign 注明克隆自 TeacherBuddy、Classes 注明克隆自 Paper LMS）；
3. 教师画像底稿（必须用调研证据修订后采用，禁止原样拍板）：
   - **画像 A · 批改减负型**：王老师，38 岁，带 2 个班约 110 人，晚自习办公室 PC 批改 2~3 小时/天；第一分钟想看"今天要批多少份、哪些学生要关注"；最怕 AI 批改不可信、误点丢数据。对应链路 L4/L5。
   - **画像 B · 出题质量焦虑型**：李老师，26 岁，新教师；第一分钟想"按课题出一套可信练习"；最怕文不对题、超纲、被学生问倒。对应链路 L2/L3。

### 1.2 需补全的调研（产出《教师端前端调研报告》）

1. **竞品**：同类 ≥5 + 间接 ≥3（方向：智学网/好分数的批改与学情页、组卷网/学科网的组卷流程、希沃白板的课堂互动、班级小管家的作业发布），每个输出**真实界面截图**（不许凭描述想象）、交互流程、差评来源；
2. **画像卡**：按底稿核验修订 2~3 张，每张回答"第一分钟想干什么 / 最怕什么"；
3. **痛点—功能映射表 + 砍单清单**：本期待办（§4）逐条挂痛点，挂不上的移入"本期不做"；
4. **技术基准**：已有 3 个 UI 克隆基准；再补 1 个 Vue3 大型应用工程基准（方向：vue-vben-admin / soybean-admin 等，调研后自选并说明理由），clone 到 `D:\frontend\research-repos\`。逐个输出《技术基准分析》（目录结构/组件划分/状态管理/API 层/测试组织/构建部署），每项工程决策三选一：直接复用 ＞ 改造复用 ＞ 自研创新。**借鉴纪律：借鉴架构思想与组件模式，禁止整套硬搬**。

**阶段 0 验收**：调研报告（竞品表 + 画像卡 + 痛点映射 + 砍单 + 技术基准分析 + **既有资产评估表**），附来源链接与截图。我确认后才进入阶段 1。

---

## 2. 阶段 1：教师端用户操作链路（骨架已预置，你需逐卡确认、补异常降级、清除断头路）

### 2.1 链路清单（8 条）

| 编号 | 链路 | 入口 | 状态 |
|---|---|---|---|
| L1 | 今日工作台 | `/teacher/today` | 可用，有硬编码假文案 |
| L2 | 备课（教案/课件） | `/teacher/prep` | **交互降级：window.prompt、采纳不落库** |
| L3 | 组卷布置 | `/teacher/assign` | 全链真实可用（工程标杆之一） |
| L4 | 批改工作台 | `/teacher/grading` | V2 可用，V1 孤儿未清 |
| L5 | 班级学情→讲评 | `/teacher/classes` | 可用，行动跳转真实 |
| L6 | 课堂互动 | `/teacher/classroom` | **半成品：题库写死、mock 404** |
| L7 | 资源库 | `/teacher/resources` | 可用，上传体验待升级 |
| L8 | Butler 管家 | 全局侧栏 | 探测式降级壳，待后端契约 |

### 2.2 黄金路径声明（资源优先倾斜）

**L3 → L4 → L5 作业闭环**：组卷布置 → AI 批改工作台逐题确认 → 班级学情/错题聚类 → 讲评行动。本期 UI 与交互打磨优先向这条链倾斜。

### 2.3 链路卡 + 接口契约锚点

> 卡中"现状断点"是架构师盘点到的**候选问题**（含证据位置），须在阶段 1 验证后方可作为靶子，发现描述有误请在汇报中纠正。契约锚点约束的是**接口形状**（路径/字段/语义），不是实现质量——组件内部实现你拥有完全修改权（按《既有资产评估表》结论执行）。

**L4 批改链（黄金路径核心）**：
- 页面：`teacher-v2/TeacherGradingV2View.vue` + `features/teacher-grading-v2/`；
- 步骤：今日深链/队列 → GET `/teacher/grading/workspace` → 逐题查看 AI 建议（suggest）→ 人工改分 review → confirm / batch-confirm → 跳班级洞察复看；
- 数据：`stores/teacher/gradingWorkspace.ts`（乐观锁 version、幂等键、Blob 文件获取 `gradingWorkspace.ts:41-47`）；
- 现状断点：V1 孤儿页 `pages/teacher/TeacherGradingView.vue` 不在路由表但文件与 `stores/teacher/grading.ts` 残留（today 深链 `TeacherTodayView.vue:219`、TeacherNav 角标 `TeacherNav.vue:24` 仍引用 V1 store）；洞察文案含硬编码假数字（见 L1）。

**L3 组卷布置链**：POST `/teacher/quizzes/generate`（题量不足守卫已在 `TeacherAssignView.vue:102,119`）→ artifacts confirm → POST `/teacher/assignments` → publish；全链真实，作为交互范本。
**L5 学情讲评链**：GET `/classes/mine` + `/classes/{id}/members` + `/teacher/classes/{id}/insights` → INSIGHT_ACTIONS 行动跳转（`TeacherClassesView.vue:142-146`）。
**L2 备课链**：GET `/teacher/lessons` → POST `/lessons/adapt` → artifacts confirm → POST `/lessons/{id}/slides` → GET download；
- 断点：环节编辑用 `window.prompt` ×3（`TeacherPrepView.vue:335/343/351`）；"采纳建议"仅本地 UI 状态无请求（`:383-393`）；"生成当堂练习"只跳转+toast（`:430-433`）；"生成板书提纲"纯前端拼 txt（`:435-447`）；班级列表绕过 classApi 直用 `api.get('/classes/mine')`（`:513`）。
**L1 今日工作台**：GET `/teacher/today`；断点：`reason: '...与最近 17/46 人失分的边界条件相关'` 硬编码（`TeacherTodayView.vue:179`），应改为渲染后端 insight 字段。
**L6 课堂链**：GET/POST `/teacher/classroom-mode`、`/teacher/classroom-session` start/question/close（`api/teacher/classroom.ts:46-63`）；
- 断点：检测题库三题硬编码（`TeacherClassroomView.vue:149-154`）；`question_no` 0 基索引歧义（`:196` → `classroom.ts:51`）；mock 未覆盖 classroom-session（`mock/teacherServer.ts:157` 只有 classroom-mode，mock 下该链 404）。
**L7 资源链**：GET/POST/DELETE `/teacher/resources*`，multipart 上传原生 fetch（`resources.ts:24-44`，401 不走刷新重试 `:39-40`）；候选题审核真实入库。
**L8 Butler 链**：POST `/teacher/butler/chat`；OPTIONS 探测 404 则明示"尚未接通后端契约"（`butler.ts:21-30`）——降级诚实，可保留模式，待后端 WRITE 放开后接真实执行反馈与流式（复用学生端 `sse.js`）。

### 2.4 断头路清单（架构师盘点候选，经你验证后在阶段 1 给出清零方案）

1. `config/features.js:11-22`、`config/nav.js:3-28` 引用不存在路由（/student/chat 等）——整文件死配置；
2. 顶栏通知铃铛无点击行为（`TeacherLayout.vue:29-33`）；
3. Classroom mock 404（无 e2e 覆盖）；
4. Prep"采纳建议/当堂练习/板书提纲"三个按钮无真实后端结果。

**阶段 1 验收**：链路卡全集逐条确认 + 断头路清零方案 + 黄金路径确认。我确认后才进入阶段 2。

---

## 3. 阶段 2：链路转化为验收标准（双轨）

1. **功能轨**：每条链路逐步骤"输入→输出 + 接口/数据断言 + 异常路径"，验证方式注明（vitest / e2e / 真实后端联调截图 / typecheck）。示例：批改 confirm 后刷新页面分数与状态持久（已有 spec，须在真后端模式复验）；
2. **体验轨**：界面与参考截图并排对比、内容真实、文案像人写的、每个按钮有真实反应；
3. **UI 施工图纸**：为教师端每个核心页面选定 1 张竞品/基准参考截图（优先从 research-repos 与阶段 0 竞品截图中选），存档到 `docs/teacher-optimize/ref-screens/` 并登记"参照点清单"（布局/密度/配色/组件形态各对标什么）；后续 UI 验收 = 与参考截图并排对比，不接受形容词描述的设计要求；
4. **内容资产清单**：演示班级/学生/作答数据使用后端真实数据（与后端对话的内容资产清单同源），禁止占位符与张三李四随机数；洞察/讲评文案必须由后端真实字段渲染；
5. **AI 产物基准库**（教师端界面内嵌 AI 生成物部分）：教案/课件预览的验收样本与后端共享基准库 `D:\math-arena\reference-repos\ai-baseline\`（后端对话建立），前端不做重复建设，但需在其就位后接入对比走查。

**阶段 2 验收**：验收用例表（链路 × 双轨）+ 参考截图集 + 内容资产清单。我确认后才进入阶段 3。

---

## 4. 阶段 3：构建实施（顺序：黄金路径 → 其余链路；每完成一条立即端到端留证）

> 本节 P0/P1/P2 是架构师基于代码盘点预判的**候选靶子**（含证据位置），不是既定结论。开工前必须逐条验证：确认属实的按此执行；判断有误或另有根因的，在汇报中推翻并给出你的方案——禁止不经验证照单执行。

### 4.1 P0 任务清单（全部挂在链路上）

1. **批改 V1/V2 双轨收敛**（L4）：架构师盘点显示 `pages/teacher/TeacherGradingView.vue` 是路由孤儿（不在路由表，仅 today 深链与 TeacherNav 角标还引用 V1 store）；你验证后自行决定收敛方案（删除/合并/改造均可），清除断头引用与死测试；
2. **假数据清零**（L1/L6）：Today 硬编码"17/46"改为渲染后端真实 insight 字段；Classroom 硬编码三题改为由已确认教案/题库接口驱动；与后端确认 `question_no` 序号语义后统一（走契约记录）；
3. **Prep 交互升级**（L2）：`window.prompt` ×3 替换为真实行内编辑组件（含 KaTeX 预览）；"采纳建议"接后端落库端点（契约新增，见后端提示词 §2.4）；"板书提纲"改走后端生成+下载；"当堂练习"跳转携带真实课题上下文（写 context store）；
4. **死配置清理**：修复/移除 `config/features.js`、`config/nav.js` 死引用；通知铃铛接真实通知源或明示"暂无通知"空态。

### 4.2 P1 任务清单

5. **API 调用收敛**：三种班级获取写法统一走 classApi（`TeacherPrepView.vue:513`、`TeacherAssignView.vue:140`、`TeacherClassesView.vue:130`）；裸 `/api/...` 字符串统一常量（`TeacherPrepView.vue:421`、`TeacherResourcesView.vue:124`、`gradingWorkspace.ts:42`、`butler.ts:24`）；resources 上传 401 接入 client 刷新重试（`resources.ts:39-40`）；
6. **渲染器共享内核**：`LatexText.vue` 与 `MarkdownView.vue` 的数学嵌套规则抽 `utils/` 共享模块，消除双轨同步负担（`LatexText.vue:53` 自述"同规则"）；
7. **mock 与 e2e 补盲区**：mock 补齐 classroom-session 与 butler 端点（`mock/teacherServer.ts`）；新增课堂链 e2e；黄金路径 e2e 在真实后端模式（E2E_MOCK=0）各跑一遍留证；
8. **Butler 流式化**（待后端契约就绪）：复用 `sse.js` + 学生端消息组件栈，替换 OPTIONS 探测式面板。

### 4.3 P2 任务清单（P0/P1 验收有余量才做）

9. 教师端主题 token 收敛（teacher.css t-token 与页内巨型 scoped CSS 双轨；暗色适配评估）；
10. 仓库卫生：清理根目录 5 套 dist*、dev*.log、vite timestamp 临时文件，补 .gitignore（.merge-backup/.learnings/.workbuddy 私有目录）。

### 4.4 实施纪律

- **设计必引基准**：新组件动手前声明"参照 research-repos 中 X 项目的 Y 组件/模式"；若你评估批改 V2 的工程模式（query 同步/乐观锁/幂等）确属高质量，可将其定为内部标杆；
- **单一内核**：同类能力一套实现（上传走 useFileUpload、班级走 classApi、渲染走共享内核）；
- **增量纪律**：改动前列出将动的文件与理由；
- **UI 照图施工**：严格按阶段 2 参考截图实现布局/密度/配色，禁止自由发挥的渐变/玻璃拟物模板风；
- **优雅降级**：后端不可用时明示降级态（沿用 Butler 的诚实探测模式），禁止白屏与静默失败；
- 每完成一条链路：页面截图 + vitest/e2e 输出落 `docs/teacher-optimize/evidence/`，再开下一条。

---

## 5. 阶段 4：链路验收（最终门禁）

- 按阶段 2 用例表逐条走查，每条链路输出 通过/不通过 + 证据；
- **AI 生成物专项**：教案/课件预览页与基准库样本并排截图对比，输出《产物对比验收表》；
- 黄金路径（L3→L4→L5）以真实用户视角从登录到讲评闭环完整走一遍（可用 web-gui-tester/playwright 录制），全程截图或录屏，并在真实后端（非 mock）上执行；
- 输出《验收报告》：用例通过率 + 证据索引 + 遗留问题清单（含严重级别）。

---

## 6. 红线（任何一条违反即返工）

1. 没有阶段 0 调研报告，禁止写任何代码；
2. 禁止凭空编造需求、界面、内容、数据——需求来自调研，界面来自参考截图，内容来自真实素材；
2b. 界面内嵌 AI 生成物未与基准库对比验收前，禁止视为完成；
2c. 禁止凭空自研——组件与模式必须参照 research-repos 已克隆项目；
3. 禁止无靶迭代——一切修改必须对应某条链路验收用例的未通过项；
4. 禁止大规模推倒重构——禁止无靶全量重写；经《既有资产评估表》判定为"改造/重写"的模块允许修改与重写，前提是：对应某条链路验收用例的未通过项、小步提交、测试护航、汇报申报；
5. 禁止只交代码不交证据——每个改动附运行时验证（截图/vitest/e2e）；
6. 禁止功能堆叠——新功能必须挂到某条链路卡；
7. 每阶段结束停下来等我验收，禁止自作主张连续推进；
8. **契约红线**：禁止在 mock 中私自"实现"不存在的后端契约冒充完成；对契约的期望变更必须走《教师端契约变更记录》。

---

## 7. 汇报格式（每阶段结束按此提交）

```
【阶段 X 完成汇报 · 前端】
1. 产出物清单（文档/组件/页面/测试文件路径）
2. 验收证据（页面截图编号 / vitest 输出 / e2e 输出 / typecheck 结果 / 联调录屏）
3. 发现的问题与取舍说明
4. 与本提示词要求的偏差（如有，说明原因；含对链路骨架/画像底稿的任何修订）
5. 契约期望变更（如对后端接口有新增/调整期望，附《教师端契约变更记录》条目）
6. 下一阶段开工前需要我确认的事项
```
