# 工作区基线与落库记录（§0.1 处置证据 · 2026-09-01）

> 对应《教师端优化提示词-后端版》§0.1「当前工作区风险」的处置留证：
> 跑通测试基线 → 分主题审查 diff（子任务报告，要点见下）→ 分组 commit 落库 → 契约登记 → 证据归档。

## 一、测试基线（全量 tests/，提交内容 = 基线内容）

- 命令：`cd services/api && M3_ENABLE_TEACHER=true python -m pytest tests -q --tb=short --durations=25`（Python 3.13.5 / pytest 9.1.1；测试库 test_math_arena@54329 由 conftest 重建 65 表 + 301 知识点种子）
- 结果：**1628 passed / 14 failed / 14 skipped，用时 1916.70s（31 分 56 秒）**
- 日志：`services/api/_baseline_full.log`（gitignore 覆盖，本记录为其归档摘要）

### 14 个失败的四类归因（全部有栈证据）

| 类别 | 用例 | 根因（证据） | 处置归属 |
|---|---|---|---|
| ① 本地代码缺陷：string.Template 占位符（8 个） | test_classroom_acceptance ×6（A/B/C/C-v1/v2/v3）、test_classroom_openmaic_contract ×2 | `ValueError: Invalid placeholder in string: line 22, col 94`——本批改写 `_safe_prompt_format`（string.Template）后，提示词模板/LLM 返回内容中的 LaTeX `$` 未转义，生成状态 failed（`assert 'failed' == 'ready'`） | **阶段 1 头号代码靶子**（L6 生成链），充值与否都存在 |
| ② 测试夹具问题（2 个） | test_classroom_history::test_list_filter_and_soft_delete、test_progress_update | `asyncpg UndefinedTableError: relation "users" does not exist`——会话内 schema/fixture 作用域问题 | 阶段 1 修夹具 |
| ③ 并行对话干扰（2 个） | test_m3_teacher_profile::test_alembic_single_head、test_teacher_grading_v2_workspace::test_grading_v2_migration_keeps_single_linear_head | `AssertionError: ['om6_classroom_practice'] != ['om5_error_record_origin']`——**另一个并行后端对话**于 12:33 在工作区放入未提交迁移 `om6_classroom_practice.py`（down_revision=om5，内容合法），使工作树 head=om6；**本次提交内容本身 head=om5，干净检出不受影响** | 用户协调并行会话；om6 落库后两测试断言需同步 om6 |
| ④ 行为断言变化（2 个） | test_student_pipeline::TestChoiceGrading::test_judge_grading（`assert 'wrong' == 'correct'`）、test_rag_trgm_scope::test_trgm_retrieves_student_textbook_when_scope_filter_is_used（`assert 3 == 1`） | 前者疑与本批 J1 判答纪律改动/真实模型输出相关；后者为 trgm 检索 scope 过滤返回了 3 行（预期 1 行），RAG 检索真实缺陷 | 阶段 1 定靶（对应链路 L3/L5 检索、学生判答） |

### 慢的根因（--durations=25 证据）

- top25 全部是课堂引擎验收/历史测试（40~278s/个）——它们走**真实生成链**：ai_calls 显示 deepseek 通道 47 次 success、**平均 20.4s/次**；
- 星辰通道 22 次 error（见下）加剧等待；spark `spark down` ×2；
- **改进候选（阶段 1）**：测试内 mock 外呼/压低测试态超时——当前套件把"外部依赖超时+重试链"当成了测试路径。

### 模型通道运行时证据（ai_calls 审计表，基线窗口 3h）

| provider | success | error/fallback | 证据 |
|---|---|---|---|
| deepseek | 47（avg 20364ms） | 3 fallback | 通道可用但慢——真实生成测试慢的主因 |
| **xingchen（星辰）** | 1 | **22** | **`20373: Model authorization error: appId has no feature authorization or business volume exceeds limit` ×15（=用户所指欠费/限额）+ `20805: OpenAPI output error` ×5** |
| spark | 1 | 2 | `spark down`（星火主通道本就未配置/不可用） |
| mimo | success | — | 13:06 仍有 success（2381ms），MiMo 正常 |

**结论：用户判断属实——星辰通道已欠费/超限；但 14 个失败无一由 API 拒绝直接造成**（星辰失败均按设计走了降级路径）。欠费的实际影响 = ①拖慢套件（重试/超时链）②所有星辰依赖链路（教案 LLM、wf_smart_quiz、错因分析）在真实运行中降级——这正是 P0-3「星辰不可用降级 ModelRouter 真生成」要解决的场景。

## 二、分组落库（6 commit + 文档 1 commit，按依赖序）

| 序 | 组 | commit | 内容摘要 |
|---|---|---|---|
| 1 | D 基建 | `a1ccf93` | GeoGebra 生成服务（修复 HEAD 悬空 import）+ files `/raw` 流式端点 + embedding 超时/代理直连/Windows 启动壳 + conftest kp 种子按树深插入 |
| 2 | B 双师课堂 | `e48e8c3` | stage_router grounded-RAG 重构（om3/om4 迁移 + math_verifier 数学验证层 + OpenMAIC 文档契约 + 人教教材导入 + 10 个课堂测试；v6/v7 实验件归档 WIP） |
| 3 | A1 学生端修复 | `ab0a975` | 错题来源 origin 链（om5 迁移）+ J1/D1/D3/J3/J4/D6 五连修 + 日练题库兜底 + head 断言改 om5 |
| 4 | A2 真题卷 | `93f6705` | real_paper 原卷性重组 + real-papers 列表 + 逐题来源透出（source_label + KP_MAP 扩码） |
| 5 | C 教师端 | `374de73` | 课堂会话 Session 端点 + 批后讲评/错题集中洞察 + 今日备课完成度（含 seed_teacher_demo） |
| 6 | E 杂项 | `617f2b8` | pg 卷改名 external + .gitignore 本地资产规则 + 阶段报告/提示词文档归档 |
| 7 | phase0 文档 | （见 git log） | 本目录调研文档 + 基线记录 + 好分数截图 |

迁移链核验：om3→om4→om5 单线（om3 实挂 m2_020_classroom_sessions，非 om1 直连；om1 仅有 EOL 噪声已还原）。与《通宵优化修复总报告》§八交叉核对一致，另发现 3 处报告未载改动（D7 日练兜底、错题图形端点、KP_MAP 扩码 set_logic/complex/inequality——§七遗留 2 称"待确认"但代码已扩）。

## 三、契约登记（先登后交，红线 8）

《教师端契约变更记录.md》本次新增：BE→FE shipped 条目 5 条（insights kind 枚举 error_cluster、today 六字段、grading image、5 个新端点、files content local 分支语义）；并对前端 5 条 FE→BE 期望逐条答复（kind SSOT 权威枚举、question_no=0 基、adopt-suggestion 端点形状、classroom-session 现状语义、V1 queue 保留兼容期）——全部 accepted，待前端对话回读。

## 四、遗留未提交（有意保留，非遗漏）

| 路径 | 判定 |
|---|---|
| `M4科研端/research-workspace/research-workspace.design` | 设计画布元数据变更，与教师端无关——留给科研端对话处置，不混入本批 |
| `services/api/alembic/versions/om6_classroom_practice.py`（12:33 出现） | **并行后端对话的在产文件**（分层练习契约），不属本会话处置；其存在使工作树 alembic head=om6（造成②类 2 个断言失败） |
| `services/api/scripts/import_gaokao_multichoice.py`（11:38 出现） | 同上，并行对话的在产导入脚本 |
| `services/api/app/domains/classroom/_clean_prompt.py` | 一次性补丁脚本（内嵌绝对路径），建议不入库；如需留档移 deliverables |
| `deliverables/teacher-phase0/` | 阶段 0 产出，随后单独提交 |

## 五、给用户的协调请求

1. **星辰充值或替换**：星辰 20373（欠费/限额）已实锤；充值前所有星辰链路按既有降级纪律运行（不会静默失败）。
2. **并行后端会话**：本会话运行期间另有对话在同一仓库写入 om6/导入脚本——请确认该会话的任务边界，避免双会话同时改 `services/api`（契约记录可作双后端协调锚点）。
3. 14 个失败为**提交前既有**（非本次落库引入），已按四类归因；阶段 1 开工将以①类 8 个课堂失败为第一靶。
