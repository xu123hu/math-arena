[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "math-arena-sandbox"
version = "0.1.0"
description = "Math Arena - SymPy 数学沙箱（M2）"
requires-python = ">=3.11"
dependencies = []
